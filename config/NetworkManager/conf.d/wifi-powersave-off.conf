# Disable WiFi power saving for SSH/stream stability (replaces disable-wifi-powersave.service).
# 2 = disable powersave; applied by NetworkManager whenever wifi connects.
[connection]
wifi.powersave = 2
